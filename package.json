{
  "scripts": {
    "prettier": "prettier --write --loglevel warn \"**/**/*.ts\"",
    "pretest": "npx tsc --incremental -p tests/tsconfig.json",
    "test": "playwright test -c tests-out --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.14.1",
    "@types/node": "^15.12.2",
    "prettier": "^2.3.2",
    "typescript": "^4.4.2"
  },
  "prettier": {
    "singleQuote": true,
    "tabWidth": 2,
    "semi": true,
    "printWidth": 80
  }
}
